# 博客文章目录

这个目录包含所有博客文章的 Markdown 文件。

## 📝 添加新博客文章

### 1. 创建 Markdown 文件

在 `blog/` 目录下创建一个新的 `.md` 文件,文件名将作为文章的 URL slug。

例如: `blog/my-new-post.md` → URL: `/post/my-new-post`

### 2. 添加 Frontmatter

每篇文章必须包含 YAML frontmatter,格式如下:

```markdown
---
title: "文章标题"
date: "2025-11-04"
author: "xlxzhc"
tags: ["标签1", "标签2", "标签3"]
category: "分类"
description: "文章描述(用于SEO和社交分享)"
featured: false
---

# 文章标题

文章正文内容...
```

### 3. Frontmatter 字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 文章标题 |
| `date` | string | ✅ | 发布日期 (YYYY-MM-DD 格式) |
| `author` | string | ✅ | 作者名称 |
| `tags` | array | ✅ | 标签列表 |
| `category` | string | ❌ | 文章分类 |
| `description` | string | ❌ | 文章描述(如果不提供,会自动生成) |
| `excerpt` | string | ❌ | 文章摘要(如果不提供,会自动生成) |
| `featured` | boolean | ❌ | 是否为精选文章(默认 false) |
| `readTime` | number | ❌ | 阅读时间(分钟,如果不提供会自动计算) |

### 4. 生成博客索引

添加或修改文章后,需要重新生成博客索引:

```bash
npm run build:blog
```

或者直接运行开发服务器(会自动生成):

```bash
npm run dev
```

## 🚀 工作流程

### 开发模式

```bash
npm run dev
```

这会:
1. 自动扫描 `blog/` 目录
2. 生成 `src/data/blog-index.json`
3. 启动开发服务器

### 构建生产版本

```bash
npm run build
```

这会:
1. 生成博客索引
2. 构建应用
3. 生成 sitemap

## 📂 文件结构

```
blog/
├── README.md                              # 本文件
├── welcome-to-my-blog.md                  # 博客文章
├── react-typescript-best-practices.md     # 博客文章
└── ...                                    # 更多文章

src/
├── data/
│   └── blog-index.json                    # 自动生成的博客索引
├── hooks/
│   └── usePosts.ts                        # 博客数据加载逻辑
└── utils/
    └── markdown.ts                        # Markdown 解析工具

scripts/
└── generate-blog-index.js                 # 博客索引生成脚本
```

## ✨ 支持的 Markdown 功能

- 标题 (H1-H6)
- 列表 (有序/无序)
- 代码块 (支持语法高亮)
- 行内代码
- 链接和图片
- 粗体和斜体
- 引用
- 表格
- 分隔线

## 💡 提示

1. **文件命名**: 使用小写字母和连字符,避免空格和特殊字符
   - ✅ `my-awesome-post.md`
   - ❌ `My Awesome Post!.md`

2. **日期格式**: 始终使用 `YYYY-MM-DD` 格式,并用引号包裹
   - ✅ `date: "2025-11-04"`
   - ❌ `date: 2025-11-04`

3. **标签**: 使用数组格式,每个标签用引号包裹
   - ✅ `tags: ["React", "TypeScript", "前端"]`
   - ❌ `tags: [React, TypeScript, 前端]`

4. **自动功能**: 
   - 如果不提供 `readTime`,系统会根据内容自动计算
   - 如果不提供 `excerpt`,系统会从内容中自动提取前150个字符

## 🔍 示例文章

参考 `blog/welcome-to-my-blog.md` 查看完整示例。

---

**最后更新**: 2025-11-04